
class clientSend()

clientSend(connect)

   address and it will return the Player ID
   to the client

player.connected
"p1.connected" - assign p1 ip address
"p1.lobby.ready" - assign p1 ready = true
"p2.lobby.ready" - assign p2 ready = true
"p2.lobby.chat.<string>" - p2 says in chat <string>



clientSent(lobby)
    send string the is 

    getPlayerID  from player class
    add .
    add lobby command
    send encoded string to server

    player = p1 



    p1.clientSend(lobby)
       string = self + ".lobby.ready"
       encode(str(string))
   


serverRecieved(connect)

class clientRecieve()

"gamestart"



"movement.p1.x4,y7" - move character for p1 right 4 space up 7 spaces
















